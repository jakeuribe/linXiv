usage: linxiv search [-h] [--source {arxiv,openalex,crossref}] [--max MAX]
                     [--local]
                     query

positional arguments:
  query                 Search query string

options:
  -h, --help            show this help message and exit
  --source {arxiv,openalex,crossref}
  --max MAX             Max results
  --local               Search the local library full-text index instead of a
                        remote source
